const DbError = require("../db/error");

const DUP_UID = "DUP_UID";
const DUP_EMAIL = "DUP_EMAIL";
const INVALID_UID = "INVALID_UID";
const INVALID_EMAIL = "INVALID_EMAIL";
const INVALID_PASSCODE = "INVALID_PASSCODE";
const INVALID_PHRASE = "INVALID_PHRASE";


module.exports = {
    DUP_UID, 
    DUP_EMAIL,
    INVALID_UID,
    INVALID_EMAIL,
    INVALID_PASSCODE,
    INVALID_PHRASE,
};
